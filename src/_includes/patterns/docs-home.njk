<div
    data-h2-margin="base(x4, auto, 0, auto)"
    data-h2-padding="base(0, 0, x2, 0) l-tablet(0, 0, x2, 0)">
  <div
    data-h2-text-align="base(center) l-tablet(left)" 
    data-h2-flex-grid="base(center, 0, x2)">
    <div data-h2-flex-item="base(1of1) l-tablet(content)">
      <img 
        src="/static/img/icon-test-tubes.svg" 
        alt="{{ home[locale].properties.iconAlt }}" 
        data-h2-width="base(25%) p-tablet(15%) l-tablet(x3)"
        data-h2-margin="base(5px, 0, 0, 0)"
        data-h2-display="base(inline-block)"
        class="section-icon">
    </div>
    <div data-h2-flex-item="base(1of1) l-tablet(auto)">
      <h3
        data-h2-font-size="base(h3, 1.3)"
        data-h2-font-weight="base(800)">What's new</h3>
    </div>
    <div data-h2-flex-item="base(1of1) l-tablet(content)">
      <a 
        href="{{ home[locale].properties.titleLink.url }}"
        title="{{ home[locale].properties.titleLink.title }}"
        data-h2-font-weight="base(300)"
        data-h2-font-color="base:hover(primary) base:dark:hover(lighter.primary)"
        data-h2-font-size="base(h6)">See the full history</a>
    </div>  
  </div>
  <div data-h2-padding="base(x2, 0, 0, 0)">
    <div data-h2-flex-grid="base(flex-start, 0, x2, x1)">
      <div 
        data-h2-flex-item="base(1of1) l-tablet(content)"
        data-h2-align-self="base(center)">
          <div
            data-h2-width="base(25%) l-tablet(x3)"
            data-h2-height="base(x.25)"
            data-h2-bg-color="base(primary) base:dark(accent)"
            data-h2-radius="base(pill)"></div>
        </div>
      <div data-h2-flex-item="base(1of1) l-tablet(fill)">
        <p 
          data-h2-font-color="base(dark.font) base:dark(white)"
          data-h2-margin="base(0)"
          data-h2-font-weight="base(700)"
          data-h2-font-size="base(h6)">Hydrogen's latest stable release is: <span data-h2-font-color="base(dark.primary) base:dark(lighter.primary)">2.0.0</span></p>
      </div>
      <div style="flex-basis: 100%; width: 0;"></div>
      <div 
        data-h2-flex-item="base(1of1) l-tablet(content)"
        data-h2-align-self="base(center)">
          <div data-h2-width="base(25%) l-tablet(x3)"></div>
        </div>
      <div data-h2-flex-item="base(1of1) l-tablet(fill)">
        <p
          data-h2-font-color="base(dark.font) base:dark(white)"
          data-h2-font-weight="base(700)">22 breaking changes</p>
        <ul data-h2-padding="base(x1, 0, 0, x1)">
          <li data-h2-margin="base(auto, auto, x.5, auto)">flex grid only takes 3 (4th optional) values now and has been reworked to avoid horizontal overflows</li>
          <li data-h2-margin="base(auto, auto, x.5, auto)">b must now be included in the config, with a null query value</li>
          <li data-h2-margin="base(auto, auto, x.5, auto)">config options have been renamed in all cases to be more specific to their expected value (e.g. value => query for media queries)</li>
          <li data-h2-margin="base(auto, auto, x.5, auto)">location is now offset</li>
          <li data-h2-margin="base(auto, auto, x.5, auto)">margin/padding/offset is now base(all), base(top-bottom, right-left), base(top, right, bottom, left)</li>
          <li data-h2-margin="base(auto, auto, x.5, auto)">separates text deco from font style</li>
          <li data-h2-margin="base(auto, auto, x.5, auto)">removes border widths from config</li>
          <li data-h2-margin="base(auto, auto, x.5, auto)">removed width and height from config</li>
          <li data-h2-margin="base(auto, auto, x.5, auto)">removed opacity from the config</li>
          <li data-h2-margin="base(auto, auto, x.5, auto)">ditches the compile script</li>
        </ul>
      </div>
    </div>
  </div>  
</div>